name: wow_classic_beta

on:
  workflow_dispatch:
  schedule:
    - cron: '00 09 * * *'

jobs:
  build:
    uses: Vladinator/wow-dbc-archive/.github/workflows/template.yml@main
    with:
      NAME: wow_classic_beta
      PRODUCT: wow_classic_beta
    runs-on: ubuntu-latest
