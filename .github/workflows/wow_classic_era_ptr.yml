name: wow_classic_era_ptr

on:
  workflow_dispatch:
  schedule:
    - cron: '00 09 * * *'

env:
  NAME: wow_classic_era_ptr
  PRODUCT: wow_classic_era_ptr

jobs:
  build:
    uses: Vladinator/wow-dbc-archive/.github/workflows/template.yml@main
    with:
      NAME: ${{ env.NAME }}
      PRODUCT: ${{ env.PRODUCT }}
    runs-on: ubuntu-latest
