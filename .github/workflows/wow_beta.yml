name: wow_beta

on:
  workflow_dispatch:
  schedule:
    - cron: '00 09 * * *'

jobs:
  build:
    uses: Vladinator/wow-dbc-archive/.github/workflows/template.yml@main
    with:
      NAME: wow_beta
      PRODUCT: wow_beta
